{
  "name": "application",
  "version": "1.0.0",
  "scripts": {
    "dev-frontend": "cd frontend && pnpm run dev",
    "dev-backend": "cd backend && source .venv/bin/activate && flask --app app run --debug",
    "dev": "pnpm run dev-frontend & pnpm run dev-backend"
  }
}
